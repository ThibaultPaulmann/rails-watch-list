<div class="container">
  <div class="list_new">
    <h1>Create a new list</h1>
    <%= simple_form_for(@list) do |f| %>
      <%= f.input :name %>
      <%= f.button :submit, class: "btn btn-outline-secondary" %>
    <% end %>
  </div>
</div>
